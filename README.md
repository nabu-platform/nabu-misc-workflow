# nabu-misc-workflow

Deprecated: retained for compatibility with existing projects.

This module remains published and supported only to avoid breaking older installations and builds that still depend on `nabu.misc:workflow`.
New development should avoid introducing new dependencies on this module.
